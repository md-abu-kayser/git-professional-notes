# git clean & git checkout --

- `git clean -fd` remove untracked files.
- `git checkout -- <file>` discard working tree changes (now `git restore <file>`).

> 📘 Next: [Recovering Lost Commits – reflog](05-recovering-lost-commits-reflog.md)
