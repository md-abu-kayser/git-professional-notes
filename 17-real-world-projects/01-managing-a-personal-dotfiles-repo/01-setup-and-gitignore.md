# Dotfiles Repo – Setup & .gitignore

Bare repo in home: `git init --bare $HOME/.cfg`. Alias: `alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'`.

> 📘 Next: [Tracking & Updating with Scripts](02-tracking-and-updating-with-scripts.md)
