# git pull & git pull --rebase

`git pull` = fetch + merge. `--rebase` rebase instead of merge. Keep history linear.

> 📘 Next: [git push – force & --force-with-lease](03-git-push-force-and-lease.md)
