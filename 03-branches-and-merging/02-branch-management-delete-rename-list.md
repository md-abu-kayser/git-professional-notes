# Branch Management

- `git branch -d <name>` (safe delete), `-D` (force).
- `git branch -m <old> <new>` rename.
- `git branch -a` list all (local + remote).

> 📘 Next: [git merge – Fast‑forward & Three‑way](03-git-merge-fast-forward-three-way.md)
